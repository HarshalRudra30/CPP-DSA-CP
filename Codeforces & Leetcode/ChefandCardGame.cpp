#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL)
#define rep(i,n) for(int i=0;i<n;i++)
using namespace std;

int sum(int x){
    int rem = 0;
    for(int i = 0; i<)
}

int main(){
fast;
    int t,a,b;
    cin>>t;
    int n;
    while(t--){
        cin>>n;
        while(n--){
            cin>>a>>b;

        }
    }
    return 0;
}